@page "/repairsource"
<h3>RepairSource</h3>

<h4>Select rule</h4>
<select @bind="rule">
    <option value="select option">Selec rule</option>
    @foreach (var (id, description) in rules)
    {
        <option value="@id">@description</option>
    }
</select>

@switch (rule)
{
    case "lineadd":
        <Olympiad.Admin.Components.RepairSource.LineAdder />
        break;
    case "reset":
        <Olympiad.Admin.Components.RepairSource.RepairReset />
        break;
    case "linecomment":
        <Olympiad.Admin.Components.RepairSource.LineCommenter />
        break;
    case "regexreplace":
        <Olympiad.Admin.Components.RepairSource.RegexReplace/>
        break;
    default:
        <h4>Select rule</h4>
        break;
}


@code {

    string rule = "select option";

    List<(string, string)> rules = new List<(string, string)>
{
        { ("lineadd", "Add line to program") },
        { ("reset", "Undo editing") },
        { ("linecomment", "Comment line") },
        { ("regexreplace", "Replace by regex") }
    };
}
