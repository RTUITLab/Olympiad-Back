<Tooltip Title="DateTimeOffset?.ToString()">
    <Text>@(DateTimeOffset?.LocalDateTime.ToString() ?? "—")</Text>
</Tooltip>
@code {
    [Parameter]
    public DateTimeOffset? DateTimeOffset { get; set; }
}
