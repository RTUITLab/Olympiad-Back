version: '3.8'

services: 
    postgres:
        image: postgres:13-alpine
        ports:
            - 5432:5432
        environment: 
            - POSTGRES_PASSWORD=password
    rabbitmq:
        image: rabbitmq
        ports:
            - 5672:5672